spring:
  application:
    name: consumer-hystrix-dashboard
  profiles:
      active: dev
server:
  port: 6085
feign:
  hystrix:
    enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:6080/eureka/


